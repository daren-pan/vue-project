# 修改清单 · 2026-08-31

> 记录当天完成的需求与改动，提交前追加。格式见 [README.md](README.md)。

---
## 上下文重构：AGENTS.md 改为大纲 + 阶段/模块/Git 文档分层

- **需求**：减少 Agent 每次自动注入的上下文。把 `AGENTS.md` 从"塞满细则"改为**精简大纲**，细则按来源分目录维护；每个阶段一个文档、开发阶段每个模块一个文档、Git 工作流独立文档。
- **改动**：
  - `AGENTS.md`：精简为大纲（§1 通用硬性规则 + §2 Agent 全流程规范 + §3 参考索引），删掉内联的模块编码规范、工作流细则、上下文打印机制。
  - 新增 `docs/modules/`：`README.md`（索引+维护规则）、`coding-standard.md`（通用编码规范，由 AGENTS.md 迁出）、`ruoyi-system.md`、`ruoyi-workflow.md`、`ruoyi-gen.md`、`ruoyi-job.md`、`ruoyi-file.md`（各模块职责/依赖/接口/编译命令）。
  - Git 工作流新增"**建立分支后立即向用户显示当前分支**"硬规则：`git branch --show-current`（worktree 用 `git worktree list`）并在回复展示分支 + 关联 issue/task。
  - 同步：`.github/copilot-instructions.md`、`agent/README.md`、`docs/changelog/README.md`。
  - 修正交叉引用：`docs/process/00/01/02/03/05-*.md`、`workflow-management.md`、`worktree-management.md`、`docs/architecture/*` 中指向旧 AGENTS.md §4/§5/§7/§8/§9 的失效链接（统一指向新位置）。
- **验证**：文档为 `.md`，无 `.java`/`.vue` 改动，不涉编译；引用一致性经 `git status`、全局 grep（无残留旧 `AGENTS.md §X` / `4.x` 引用）核对通过。
- **提交**：（提交后回填 commit sha）

---

## 工作流模块演示：InstanceStatusVO 增加 businessKey 字段

- **需求**：按文档流程完整走一遍 Agent 开发流程（判阶段 → 强制先读 → 建分支 → 改代码 → 编译 → 留痕 → 提交）。
- **改动**：
  - `ruoyi-modules/ruoyi-workflow/.../domain/vo/InstanceStatusVO.java`：新增 `businessKey`（关联业务单据）字段 + 标准 getter/setter，每字段带 `/** 注释 */`。
- **验证**：`mvn compile -pl ruoyi-modules/ruoyi-workflow -am -q` 通过，`InstanceStatusVO.class` 已生成。
- **提交**：（提交后回填 commit sha）

---

## auto_PRM 流程演示：TaskVO 增加 assignee 字段

- **需求**：全自动化跑通「建分支 → 改代码 → 编译 → 提交 → push → API 建 PR → 等 CI → 自动合并 → 删分支」，演示 GitHub API + PAT 驱动的自动 PR 流程（squash 合并）。
- **改动**：
  - `ruoyi-modules/ruoyi-workflow/.../domain/vo/TaskVO.java`：新增 `assignee`（审批人/处理人）字段 + 标准 getter/setter，每字段带 `/** 注释 */`。
- **验证**：`mvn compile -pl ruoyi-modules/ruoyi-workflow -am -q` 通过（TaskVO.class 已生成）。
- **提交**：（提交后回填 commit sha）

---

## Git 工作流补齐「先审后推」与「PR 正文 UTF-8」规则

- **需求**：①改完代码须先给用户复核、确认后再推送；②创建 PR 时备注不能乱码（此前 PR #3 正文因非 UTF-8 发送变成 `??`）。
- **改动**：
  - `AGENTS.md` §2.5 新增规则 7（先审后推）、规则 8（PR 正文 UTF-8）。
  - `.github/copilot-instructions.md` §2.5 同步新增规则 7/8。
  - `docs/process/workflow-management.md`：新增 §0.5（改动确认与 PR 编码，必须）、§4.1 门禁新增两条自检项。
- **验证**：纯 `.md` 改动，不涉编译；diff 已提交前核对。
- **提交**：（提交后回填 commit sha）

---

## Git 工作流规则 8 收紧 + 新增「分支保留约两周」规则 9

- **需求**：①收紧规则 8——合并接口的 `commit_title`/`commit_message` 也必须 UTF-8（此前 PR #3/#4 合并提交信息乱码）；②新增规则 9——合并/推送后分支**不要立即删除**，修改代码建立的分支**保留约两周，超过两周且未再使用才删除**。
- **改动**：
  - `AGENTS.md` §2.5：规则 5 改"分支保留按规则 9"、规则 8 收紧（含合并接口 UTF-8）、新增规则 9（分支保留期）。
  - `.github/copilot-instructions.md` §2.5：同步。
  - `docs/process/workflow-management.md`：新增 §0.6（分支保留期，必须）、§0.5 收紧、§0.3/§4.2 改"立即删除"→"保留约两周"。
  - `docs/process/worktree-management.md` §6：清理流程改为"保留约两周，超期且未使用才删"。
- **验证**：纯 `.md` 改动，不涉编译；diff 已提交前核对。
- **提交**：（提交后回填 commit sha）

---

## Git 工作流新增规则 10「推送/建分支前先拉远端对齐基线」

- **需求**：此前推送前未先拉远端，PR #4 因基线落后与远端 springboot3 冲突（changelog 同改）。新增规则：建分支前与 push 前先 fetch 并把本地基线 fast-forward 到远端最新。
- **改动**：
  - `AGENTS.md` §2.5：新增规则 10。
  - `.github/copilot-instructions.md` §2.5：同步规则 10。
  - `docs/process/workflow-management.md` §0.1：新增"建分支前先拉远端对齐基线"条目（含示例命令）。
- **验证**：纯 `.md` 改动，不涉编译；已演示 fetch origin springboot3 与本地同步、无需 ff。
- **提交**：（提交后回填 commit sha）

---

## Git 工作流规则 10 硬化（禁止裸 URL fetch + 双远端说明）

- **需求**：审计"幽灵 ahead / 跟踪引用过期"问题。结论：文档化流程全部用 `origin`、无裸 URL；但为防再犯，明确禁止裸 URL `git fetch <url>`（只写 FETCH_HEAD、不更新跟踪引用，`git status` 误显 `[ahead N]`），并说明双远端独立。
- **改动**：
  - `AGENTS.md` §2.5 规则 10：加"统一用 `git fetch origin`/`git pull origin <branch>`，禁止裸 URL `git fetch <url>`；同步后 `git status -sb` 复核"。
  - `.github/copilot-instructions.md` §2.5：同步。
  - `docs/process/workflow-management.md` §0.1：同款约束 + 双远端（fetch→github、push→github+codeup）相互独立说明。
- **验证**：纯 `.md` 改动，不涉编译；diff 已提交前核对。
- **提交**：（提交后回填 commit sha）

---

## Git 工作流新增规则 11「合并后切回主分支并拉取最新」

- **需求**：把"合并完切回主分支、拉取最新"写成硬性步骤，确保本地与远端一致、后续开发基线最新（并消除此前"HEAD 对了但跟踪引用没对"的幽灵 ahead）。
- **改动**：
  - `AGENTS.md` §2.5 新增规则 11。
  - `.github/copilot-instructions.md` §2.5：同步规则 11。
  - `docs/process/workflow-management.md`：新增 §0.7（必须），含示例 `git checkout springboot3 && git pull origin springboot3`，并强调勿用裸 URL。
- **验证**：纯 `.md` 改动，不涉编译；diff 已提交前核对。
- **提交**：（提交后回填 commit sha）

