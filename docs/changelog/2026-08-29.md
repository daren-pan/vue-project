# 修改清单 · 2026-08-29

> 记录当天完成的需求与改动，提交前追加。格式见 [README.md](README.md)。

---

## 19:xx 重构工程骨架与研发体系落地

- **需求**：以 RuoYi-Cloud 为原型，重构微服务工程 + 全套流程/模块文档 + Agent 工具链。
- **改动**：
  - 新建工程骨架：根/模块 POM（24 个）、`ruoyi-gateway/auth/system/common/api/visual` 模块结构、`ruoyi-ui` Vue3 前端。
  - 后端样例：`common-core`（R/BaseController/ServiceException/GlobalExceptionHandler）、`api-system`（RemoteUserService Feign）、`system` 纵向切片（SysUser Controller→Service→Mapper→domain + 单测）。
  - 文档：`docs/architecture/*`、`docs/process/*`、`docs/modules/*`、`docs/ops/*`、`docs/design/README.md`。
  - Agent 工具链：`AGENTS.md`、`agent/README.md`、`agent/skills/*`、`agent/mcp/*`。
  - 部署：`docker/docker-compose.yml`、`config/*`、`sql/*`、`bin/*`、`.github/workflows/ci.yml`。
- **验证**：POM XML 全部通过；文档相对链接 356 处 0 断链；`bin/check-links.sh` / `health-check.ps1` 就绪。
- **提交**：（提交后回填 commit sha）

---

## 19:xx 新增「按审批中单据查询完整审批流程」动作（ruoyi-workflow）

- **需求**：在工作流增加后端审批动作，调用时可直接通过**审批中的单据**（businessKey）把**整个流程**列出来。
- **改动**：
  - `ruoyi-modules/ruoyi-workflow/.../WorkflowApplication.java`：启动类（dynamic-datasource 接管，数据源排除修正）。
  - `ruoyi-modules/ruoyi-workflow/.../controller/WorkflowInstanceController.java`：`GET /instance/business/{businessKey}`。
  - `ruoyi-modules/ruoyi-workflow/.../service/IWorkflowInstanceService.java`、`service/impl/WorkflowInstanceServiceImpl.java`：`listFlowByBusinessKey`。
  - `ruoyi-modules/ruoyi-workflow/.../service/FlowableService.java`：`getFlowByBusinessKey`（引擎门面，History/Runtime/Task/Repository）。
  - `ruoyi-modules/ruoyi-workflow/.../domain/vo/ProcessInstanceVO.java`、`TrackNodeVO.java`：强类型 VO。
  - `ruoyi-modules/ruoyi-workflow/.../resources/application.yml`：独立库 `ry-flowable` 配置。
  - 依赖/监控修复：根 `pom.xml` 增加 Spring Boot Admin 版本管理。
  - 文档同步：`docs/modules/ruoyi-workflow.md`（§7.2 接口、§5.1/5.4 方法）、`agent/skills/workflow-dev/SKILL.md`。
- **验证**：`mvn -pl ruoyi-modules/ruoyi-workflow -am compile -DskipTests` → `BUILD SUCCESS`（✅ 编译通过）；修复 Flowable `Comment` 包名后无编译错误。
- **提交**：（提交后回填 commit sha）

---

## 16:00 增加「系统自动通过 / 推进到下一节点」动作（ruoyi-workflow）

- **需求**：`ProcessInstanceController` 增加一个方法，仅传入单据（流程实例）ID，即可自动通过当前审批节点、推进到下一节点，无需审批人手动操作。
- **改动**：
  - `ruoyi-modules/ruoyi-workflow/.../controller/ProcessInstanceController.java`：新增 `POST /instance/{processInstanceId}/auto-advance`。
  - `ruoyi-modules/ruoyi-workflow/.../service/IWorkflowInstanceService.java`：新增 `TaskResult autoAdvance(String processInstanceId)`。
  - `ruoyi-modules/ruoyi-workflow/.../service/impl/WorkflowInstanceServiceImpl.java`：实现 `autoAdvance`（校验实例运行中 + 当前节点有待办任务；逐个复用 `IWorkflowTaskService.approve(taskId, "系统自动通过")` 推进，已处理任务跳过；计算 `processFinished`）。
- **验证**：`mvn compile -pl ruoyi-modules/ruoyi-workflow -am -q 2>&1` → 无错误输出（✅ 编译通过）；`mvn test -pl ruoyi-modules/ruoyi-workflow -am -Dtest=WorkflowInstanceServiceImplTest -DforkCount=0` → `Tests run: 5, Failures: 0, Errors: 0`（BUILD SUCCESS）。
- **新增单测**：`ruoyi-modules/ruoyi-workflow/.../service/impl/WorkflowInstanceServiceImplTest.java`（JUnit5 + Mockito，`autoAdvance` 5 个分支：实例不存在/已结束/无待办/推进至结束/全部审批失败）。
- **测试脚手架**：`ruoyi-modules/ruoyi-workflow/pom.xml` 增加 `spring-boot-starter-test`（test scope）；`src/test/resources/mockito-extensions/org.mockito.plugins.MockMaker` 使用 subclass mock maker（无 Java agent，适配受限/无代理环境）。
- **提交**：（提交后回填 commit sha）

---

## 20:xx 系统自动通过时打印单据名称与 ID（ruoyi-workflow）

- **需求**：调用 `autoAdvance` 自动推进单据时，打印当前单据名称与 ID，便于定位正在自动推进的单据。
- **改动**：
  - `ruoyi-modules/ruoyi-workflow/.../service/impl/WorkflowInstanceServiceImpl.java`：`autoAdvance` 中调用 `flowableService.getProcessNameByInstance(processInstanceId)` 获取单据名称，并用 `System.out.println` 打印「单据名称 / 单据id」。
- **验证**：`mvn compile -pl ruoyi-modules/ruoyi-workflow -am -q 2>&1` → 无错误输出（✅ 编译通过）。
- **提交**：（提交后回填 commit sha）

---

## 20:xx 清理指令文档中的 `.claude/skills` 引用

- **需求**：`.claude/skills/` 目录已删除，清理指令文档中对该路径的所有引用。
- **改动**：
  - 删除 `.claude/skills/ruoyi-workflow-dev/SKILL.md`、`.zcode/skills/ruoyi-workflow-dev/SKILL.md`。
  - `.github/copilot-instructions.md`、`AGENTS.md`：移除「模块开发细节见 `.claude/skills/...`」、0.1 第 3 条 `.claude/skills/<domain>-dev/SKILL.md`、工具链「工作流专项另见 `.claude/skills/...`」（保留 `agent/skills/`）。
- **验证**：全项目 `**/*.md` 搜索 `.claude/skills` 无匹配。
- **提交**：（提交后回填 commit sha）

---

## 20:xx 移除本地 mock 配置 MockMaker（不入库）

- **需求**：`org.mockito.plugins.MockMaker`（subclass mock maker）为本地 mock 配置，不应提交到 git。
- **改动**：
  - 删除 `ruoyi-modules/ruoyi-workflow/.../src/test/resources/mockito-extensions/org.mockito.plugins.MockMaker`。
  - `.gitignore`：新增忽略规则 `**/mockito-extensions/org.mockito.plugins.MockMaker`。
- **验证**：`git status` 确认该文件不再被跟踪；仅测试资源与忽略规则变更，无需编译。
- **提交**：（提交后回填 commit sha）
