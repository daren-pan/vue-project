# 本地开发配置模板（auth 模块只需 Redis）
# 用法：cp application-dev.example.yml application-dev.yml
# application-dev.yml 已在 .gitignore 中，不会被提交

spring:
  data:
    redis:
      host: localhost
      port: 6379
      password: <你的密码>
