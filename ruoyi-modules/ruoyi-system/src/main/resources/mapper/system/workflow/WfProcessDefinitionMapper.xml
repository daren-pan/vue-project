<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.workflow.WfProcessDefinitionMapper">

    <resultMap type="com.ruoyi.system.domain.workflow.WfProcessDefinition" id="WfProcessDefinitionResult">
        <id property="id" column="id"/>
        <result property="processKey" column="process_key"/>
        <result property="processName" column="process_name"/>
        <result property="version" column="version"/>
        <result property="status" column="status"/>
        <result property="delFlag" column="del_flag"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="remark" column="remark"/>
    </resultMap>

    <sql id="selectVo">
        SELECT id, process_key, process_name, version, status, del_flag, create_by, create_time, update_by, update_time, remark
        FROM wf_process_definition
    </sql>

    <select id="selectWfProcessDefinitionById" parameterType="Long" resultMap="WfProcessDefinitionResult">
        <include refid="selectVo"/> WHERE id = #{id}
    </select>

    <select id="selectWfProcessDefinitionByKey" parameterType="String" resultMap="WfProcessDefinitionResult">
        <include refid="selectVo"/>
        WHERE process_key = #{processKey} AND del_flag = '0' AND status = '0'
        ORDER BY version DESC LIMIT 1
    </select>

    <select id="selectWfProcessDefinitionList" parameterType="com.ruoyi.system.domain.workflow.WfProcessDefinition" resultMap="WfProcessDefinitionResult">
        <include refid="selectVo"/>
        <where>
            del_flag = '0'
            <if test="processKey != null and processKey != ''">
                AND process_key LIKE CONCAT('%', #{processKey}, '%')
            </if>
            <if test="processName != null and processName != ''">
                AND process_name LIKE CONCAT('%', #{processName}, '%')
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
        ORDER BY id DESC
    </select>

    <insert id="insertWfProcessDefinition" parameterType="com.ruoyi.system.domain.workflow.WfProcessDefinition" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO wf_process_definition(process_key, process_name, version, status, del_flag, create_by, create_time, update_by, update_time, remark)
        VALUES (#{processKey}, #{processName}, #{version}, #{status}, '0', #{createBy}, SYSDATE(), #{updateBy}, SYSDATE(), #{remark})
    </insert>

    <update id="updateWfProcessDefinition" parameterType="com.ruoyi.system.domain.workflow.WfProcessDefinition">
        UPDATE wf_process_definition
        SET process_key = #{processKey}, process_name = #{processName}, version = #{version},
            status = #{status}, update_by = #{updateBy}, update_time = SYSDATE(), remark = #{remark}
        WHERE id = #{id}
    </update>

    <update id="deleteWfProcessDefinitionById" parameterType="Long">
        UPDATE wf_process_definition SET del_flag = '2' WHERE id = #{id}
    </update>

</mapper>
