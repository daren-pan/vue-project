<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.workflow.WfNodeDefinitionMapper">

    <resultMap type="com.ruoyi.system.domain.workflow.WfNodeDefinition" id="WfNodeDefinitionResult">
        <id property="id" column="id"/>
        <result property="processDefId" column="process_def_id"/>
        <result property="nodeId" column="node_id"/>
        <result property="nodeName" column="node_name"/>
        <result property="nodeType" column="node_type"/>
        <result property="assigneeType" column="assignee_type"/>
        <result property="assigneeValue" column="assignee_value"/>
        <result property="approveMode" column="approve_mode"/>
        <result property="approveCount" column="approve_count"/>
        <result property="auditType" column="audit_type"/>
        <result property="timeoutHours" column="timeout_hours"/>
        <result property="timeoutAction" column="timeout_action"/>
        <result property="actions" column="actions"/>
        <result property="sortOrder" column="sort_order"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <sql id="selectVo">
        SELECT id, process_def_id, node_id, node_name, node_type, assignee_type, assignee_value,
               approve_mode, approve_count, audit_type, timeout_hours, timeout_action, actions, sort_order, create_by, create_time
        FROM wf_node_definition
    </sql>

    <select id="selectWfNodeDefinitionById" parameterType="Long" resultMap="WfNodeDefinitionResult">
        <include refid="selectVo"/> WHERE id = #{id}
    </select>

    <select id="selectWfNodeDefinitionByProcessDefId" parameterType="Long" resultMap="WfNodeDefinitionResult">
        <include refid="selectVo"/> WHERE process_def_id = #{processDefId} ORDER BY sort_order
    </select>

    <select id="selectByProcessAndNodeId" resultMap="WfNodeDefinitionResult">
        <include refid="selectVo"/> WHERE process_def_id = #{processDefId} AND node_id = #{nodeId}
    </select>

    <select id="selectWfNodeDefinitionList" parameterType="com.ruoyi.system.domain.workflow.WfNodeDefinition" resultMap="WfNodeDefinitionResult">
        <include refid="selectVo"/>
        <where>
            <if test="processDefId != null">AND process_def_id = #{processDefId}</if>
            <if test="nodeName != null and nodeName != ''">AND node_name LIKE CONCAT('%', #{nodeName}, '%')</if>
        </where>
        ORDER BY sort_order
    </select>

    <insert id="insertWfNodeDefinition" parameterType="com.ruoyi.system.domain.workflow.WfNodeDefinition" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO wf_node_definition(process_def_id, node_id, node_name, node_type, assignee_type, assignee_value,
            approve_mode, approve_count, audit_type, timeout_hours, timeout_action, actions, sort_order, create_by, create_time)
        VALUES (#{processDefId}, #{nodeId}, #{nodeName}, #{nodeType}, #{assigneeType}, #{assigneeValue},
            #{approveMode}, #{approveCount}, #{auditType}, #{timeoutHours}, #{timeoutAction}, #{actions}, #{sortOrder}, #{createBy}, SYSDATE())
    </insert>

    <update id="updateWfNodeDefinition" parameterType="com.ruoyi.system.domain.workflow.WfNodeDefinition">
        UPDATE wf_node_definition
        SET node_id = #{nodeId}, node_name = #{nodeName}, node_type = #{nodeType},
            assignee_type = #{assigneeType}, assignee_value = #{assigneeValue},
            approve_mode = #{approveMode}, approve_count = #{approveCount},
            audit_type = #{auditType}, timeout_hours = #{timeoutHours}, timeout_action = #{timeoutAction},
            actions = #{actions}, sort_order = #{sortOrder}
        WHERE id = #{id}
    </update>

    <delete id="deleteWfNodeDefinitionByProcessDefId" parameterType="Long">
        DELETE FROM wf_node_definition WHERE process_def_id = #{processDefId}
    </delete>

</mapper>
